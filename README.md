# D&D Campaign Platform

Веб-платформа для русскоязычных игроков и мастеров **D&D 5e**: живой лист персонажа с прокачкой в духе Baldur's Gate 3, кампании-визитки и торговцы с настоящей экономикой.

Продукт работает **до и после игровой сессии**, а не во время неё. Единственный сценарий «за столом» — игрок с телефона смотрит и прокачивает лист и закупается у торговца по ссылке от мастера.

**Это не VTT.** Не замена Roll20/Foundry, не система бросков и боёвки, не опенсорс-проект.

## Статус

Проект переписывается с нуля. Прежний прототип (Next.js-генератор персонажей) удалён — задача `DND-000` закрыта. Сейчас в репозитории только документация, конвенции и агентские воркфлоу; кода приложения ещё нет.

Следующая задача — **`DND-001` · Каркас backend и окружение**. Порядок работ и зависимости — в [`.claude/docs/BACKLOG.md`](.claude/docs/BACKLOG.md), статус задач — на [доске проекта](https://github.com/users/Dezodemius/projects/11).

## Что входит в MVP

Вход через Яндекс / VK ID (регистрация тем же действием) · пошаговое создание персонажа с автоматическим расчётом модификаторов, КД, хитов · лист персонажа mobile-first: HP, инвентарь, заклинания, кошелёк · прокачка визардом с историей уровней и откатом последнего · кампании с инвайт-кодом и read-only листами для мастера · торговцы с витриной по ссылке, покупкой и продажей · справочный контент — JSON-паком из файла, который правит только владелец.

Вне скоупа MVP (фаза 2+): AI-генераторы, хоумбрю-контент мастеров (кроме заклинаний игрока), мультикласс, лог транзакций, автоконвертация валют, мультиязычность, real-time. Полный список — в [`REQUIREMENTS.md`](.claude/docs/REQUIREMENTS.md) §6.

MVP считается готовым, когда владелец проведёт одну реальную кампанию (мастер + 5 игроков) полностью в системе, без единой ручной правки БД.

## Стек

Стек зафиксирован по итогам дискавери; альтернативы не рассматриваются.

| Слой | Технологии |
|------|-----------|
| Backend | Python 3.12, FastAPI, SQLAlchemy 2 (async), Alembic, Pydantic v2, pytest |
| БД | PostgreSQL 16 |
| Frontend | React 18 + TypeScript + Vite, TanStack Query, react-hook-form + zod, React Router, i18next (locale `ru`) |
| Инфра | Docker Compose, GitHub Actions, Cloudflare |

Backend — модульный монолит: `app/{core,auth,content,characters,campaigns,merchants}`, в каждом модуле `router.py → service.py → models.py + schemas.py`. Правила 5e живут только в `app/characters/rules_5e.py` (чистые функции); фронт их не дублирует, а берёт готовый блок `computed` из API.

## Документация

Канонические документы — в `.claude/docs/`. При конфликте: требования главнее в части «что», архитектура — в части «как».

| Документ | О чём |
|----------|-------|
| [`REQUIREMENTS.md`](.claude/docs/REQUIREMENTS.md) | Бизнес-требования: роли, сценарии US-1…US-12, границы MVP, критерии приёмки |
| [`ARCHITECTURE.md`](.claude/docs/ARCHITECTURE.md) | Архитектура: доменная модель, схема БД, API-контракт, карта экранов, roadmap |
| [`BACKLOG.md`](.claude/docs/BACKLOG.md) | Задачи `DND-NNN`: скоуп, зависимости, критерии приёмки |
| [`CLAUDE.md`](.claude/docs/CLAUDE.md) | Правила работы агента и Definition of Done |
| [`.claude/skills/`](.claude/skills) | Конвенции проекта: `code-style`, `security-review`, `ux-convention` |

## Разработка

### Команды

```bash
cp .env.example .env                # локальный конфиг: заполнить пустые значения (см. ниже)
pip install -r requirements-dev.txt # зависимости backend

docker compose up -d --build        # всё приложение: postgres + api + фронт, http://localhost:8080
alembic upgrade head                # миграции (локально, вне контейнера)
pytest                              # тесты backend
pytest --cov=app --cov-report=term-missing   # тесты backend + покрытие (htmlcov/ — отчёт в браузере)
ruff check .                        # линт backend

cd frontend && npm install                            # зависимости фронта (.env не нужен, см. frontend/README.md)
cd frontend && npm run dev                            # фронт локально, http://localhost:5173
cd frontend && npm run build                          # tsc -b && vite build
cd frontend && npm run test                            # тесты фронта (vitest)
cd frontend && npm run test:coverage                   # тесты фронта + покрытие (coverage/ — отчёт в браузере)
```

### Два режима фронта

**`docker compose up`** поднимает приложение целиком — это то же, что крутится на проде. Образ фронта многостадийный: Node собирает бандл (`npm run build`), готовую статику раздаёт nginx, и он же проксирует `/api/v1` на контейнер `api`. Браузер живёт на одном origin `http://localhost:8080` — CORS не нужен, refresh-cookie остаётся first-party. Node в финальном образе нет.

**`npm run dev`** — для разработки: Vite с hot-reload на `http://localhost:5173`, ходит на API по `VITE_API_BASE_URL` из `frontend/.env` (кросс-origin, поэтому backend отдаёт CORS-заголовки). Порты разные, так что dev-сервер и контейнер спокойно работают одновременно.

Важно: `VITE_*` переменные Vite вшивает в бандл **на этапе сборки**, а не читает в рантайме. Поэтому в образ `VITE_API_BASE_URL` намеренно не передаётся, а `frontend/.env` исключён через `.dockerignore` — иначе в собранный бандл попал бы чей-то `localhost:8000`. Без переменной клиент падает на относительный `/api/v1` (`frontend/src/api/client.ts`), который и проксирует nginx.

### Секреты

Дефолтных паролей в репозитории нет. После `cp .env.example .env` нужно сгенерировать пароль постгреса

```bash
python -c "import secrets; print(secrets.token_urlsafe(24))"
```

и подставить его в `.env` в три места: `POSTGRES_PASSWORD`, `DATABASE_URL`, `TEST_DATABASE_URL`. Пока `POSTGRES_PASSWORD` пуст, `docker compose up` не стартует, а приложение без `DATABASE_URL` падает на старте — оба варианта громкие и намеренные: тихого фолбэка на известные креды быть не должно. Порты `5432`, `8000` и `8080` публикуются только на `127.0.0.1`: наружу в проде смотрит Cloudflare и хостовый прокси, а не контейнеры.

### Ветки и задачи

База — **`develop`**. Одна задача = одна GitHub-ишью = одна ветка `feat/dnd-NNN` = один PR в `develop`. В `main` попадают только релизные мержи; прод деплоится отдельным CD-воркфлоу.

Ишью заводятся из бэклога скриптом:

```bash
python .claude/scripts/create-issues.py           # dry run
python .claude/scripts/create-issues.py --apply   # создать метки и ишью
```

`BACKLOG.md` — спецификация, а не трекер: статуса задач в нём нет. Прогресс живёт в GitHub — метки ишью (`blocked` → `agent-ready` → закрыта) и поле Status на доске проекта.

### Автоматизация

Воркфлоу осталось два:

- `.github/workflows/ci.yml` — линт и тесты backend, typecheck и сборка фронта на каждый PR.
- `.github/workflows/cd.yml` — прод: мерж в `main` → сборка образов в GHCR и деплой по SSH на app01.

> Автоматика вокруг ишью удалена целиком: ночной агент (`agent.yml`), ревьюер (`review.yml`), планировщик очереди (`plan.yml`), агент-исправитель (`fix.yml` + `fix-worker.yml`), закрытие ишью по мержу (`done.yml`) и нумерация ишью (`auto-number-issue.yml`). Скрипты `.claude/scripts/` живы и запускаются руками — см. `.claude/docs/CLAUDE.md`.

Деплой стенда (`cd-stand.yml`) тоже удалён. Если понадобится dev-стенд, он ставится отдельным воркфлоу рядом с `cd.yml`: имена `STAND_*` свободны, а раскатку на разовую машину удобно делать через `workflow_dispatch` с адресом во входном параметре, не заводя ничего в настройках репозитория.

### Прод (app01, timeweb.cloud)

Адрес сервера и его ключ хоста лежат в **секретах**, а не в переменных: репозиторий публичный, а GitHub Actions печатает блок `env` каждого шага в лог — переменная утекла бы открытым текстом и выдала бы origin-адрес за Cloudflare. Секрет маскируется звёздочками. По той же причине воркфлоу нигде не echo-ит адрес.

| Имя | Тип | Обязательна | Значение |
|---|---|---|---|
| `PROD_HOST` | secret | да | IP или домен прод-сервера |
| `PROD_SSH_KNOWN_HOSTS` | secret | да | вывод `ssh-keyscan -p 22 <IP>`: ключ хоста пинится, чтобы деплой вместе с доступом к серверу не уехал к перехватившему адрес |
| `PROD_SSH_KEY` | secret | да | приватный ключ, публичная часть — в `~/.ssh/authorized_keys` на сервере |
| `PROD_USER` | variable | нет | пользователь SSH, по умолчанию `root` (как на серверах timeweb) |
| `PROD_SSH_PORT` | variable | нет | порт SSH, по умолчанию `22` |
| `PROD_PATH` | variable | нет | каталог на сервере, по умолчанию `/opt/dnd-prod` |

Прод не собирает образы и не хранит исходники: `build` пушит в GHCR, на сервер уезжают только `docker-compose.yml` и init-скрипт для БД. `.env` с реальными `POSTGRES_*` и `JWT_SECRET_KEY` лежит на сервере и деплоем не перезаписывается.

Цепочка: `build` → pull образов → `alembic upgrade head` → `up -d` → проверка контейнеров изнутри сервера (`/healthz` через nginx фронта, SPA-шелл, `version.json` с sha коммита). Не прошла — контейнеры откатываются на тег из `.last_good_tag`; миграции не откатываются, схема должна расти совместимо вперёд. Порты публикуются только на `127.0.0.1`, наружу отдаёт хостовый nginx.

### Что важно помнить при работе с кодом

- Любая правка схемы БД — только миграцией Alembic.
- Деньги — три независимых целых поля (`gold` / `silver` / `copper`), никакой автоконвертации.
- Покупка и продажа у торговца — одна транзакция БД с `SELECT ... FOR UPDATE`.
- Level-up хранится дельтами (`LevelUpRecord`); откатывается только верхняя запись, поле `level` напрямую не редактируется.
- Проверка владения ресурсом — в каждом сервис-методе: IDOR — главный класс уязвимостей проекта.
- Все строки UI — через словарь i18next; хардкод русского текста в JSX запрещён.
- Комментарии в коде и тексты коммитов — на английском, пользовательские строки — на русском.

Полный свод — [`.claude/docs/CLAUDE.md`](.claude/docs/CLAUDE.md).

## Лицензия и контент

Проект не является опенсорсом. Игровой контент D&D загружает владелец инстанса сам: JSON-пак лежит файлом (`CONTENT_PACK_PATH`, по умолчанию `content/content-pack.json`), приложение читает его на старте, а обновляется он через закрытую браузерную админку `/internal/admin/content-import`.
