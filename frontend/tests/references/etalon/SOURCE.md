# Printable-sheet reference images

`page1.png` through `page4.png` were copied unchanged from the untracked
reference files in Claude's `feat/dnd-104-108` worktree on 2026-09-06. They
are supplied design references, not snapshots generated by this test suite and
not a historical baseline from the deleted Next.js application.

`SHA256SUMS` records the immutable source bytes. Do not regenerate or replace
these files to make a visual comparison pass; investigate and report any
mismatch instead.
