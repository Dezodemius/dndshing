# Backlog

Единица работы: **одна задача = одна GitHub-ишью = один запуск агента = один PR**.

Формат машиночитаемый — `.claude/scripts/create-issues.py` парсит его и создаёт ишью:
- заголовок задачи: `### DND-NNN · Название`
- строка `labels:` — метки через запятую
- строка `depends:` — ID задач-зависимостей (или `—`)
- остальное до следующего `###` — тело ишью.

**Статуса задач здесь нет и не будет.** Этот файл — спецификация (скоуп, зависимости, критерии приёмки); прогресс живёт в GitHub: метки ишью (`agent-ready` / `in-progress` / `blocked`) и поле Status на [доске проекта](https://github.com/users/Dezodemius/projects/11). Правь BACKLOG, только когда меняются скоуп, зависимости или метки задачи.

Каждая задача самодостаточна при условии, что агент прочитал `.claude/docs/CLAUDE.md`, `.claude/docs/REQUIREMENTS.md` (BR) и `.claude/docs/ARCHITECTURE.md` (AR).

---

## EPIC E0 · Инфраструктура

### DND-000 · Удаление легаси Next.js-прототипа
labels: epic:e0, infra
depends: —

**Контекст:** в репозитории лежит старый прототип (Next.js-генератор персонажей LSS), который выводится из эксплуатации и занимает корневой `app/`, конфликтуя с новой backend-структурой. Закрывается до DND-001.
**Скоуп:** удалить весь код старого прототипа (через `git rm`): каталоги `app/`, `features/`, `shared/`, `mockup/`, `tests/`, `test-results/`, `supabase/`; файлы `middleware.ts`, `next.config.mjs`, `next-env.d.ts`, `postcss.config.mjs`, `tailwind.config.ts`, `eslint.config.mjs`, `vitest.config.ts`, `playwright.config.ts`, `tsconfig.json`, `tsconfig.tsbuildinfo`, `package.json`, `package-lock.json`, `vercel.json`, `AGENTS.md`, `dndshing.code-workspace`, `.env.example`; старый CI `.github/workflows/ci.yml`. Почисти `.gitignore` от правил под старый стек (общие оставь).
**ОБЯЗАТЕЛЬНО сохранить (не трогать):** `.github/workflows/agent.yml`, `.github/workflows/review.yml`; всю папку `.claude/`; корневой `CLAUDE.md`; `.git*`-файлы, `.editorconfig`.
**Вне скоупа:** создание нового backend/frontend-каркаса (DND-001, DND-003) — только удаление.
**Приёмка:** в репозитории не осталось файлов старого Next.js-прототипа; `agent.yml`, `review.yml` и `.claude/` целы; после коммита `git status` чистый; ветка `feat/dnd-000`, PR в develop. После мержа корень готов под новую структуру из DND-001.

### DND-001 · Каркас backend и окружение
labels: epic:e0, backend
depends: DND-000

**Скоуп:** структура `app/{core,auth,content,characters,campaigns,merchants}` по AR §3; FastAPI-приложение с конфигом из env (pydantic-settings); `GET /healthz`; Docker Compose (api + postgres 16); Alembic инициализирован (пустая базовая миграция); pytest настроен с тестовой БД; README с командами запуска.
**Вне скоупа:** любые бизнес-модели и эндпоинты.
**Приёмка:** `docker compose up` поднимает api+БД; `/healthz` отвечает 200; `alembic upgrade head` проходит на пустой БД; `pytest` зелёный (хотя бы smoke-тест healthz); секция «Команды» в CLAUDE.md заполнена реальными командами.

### DND-002 · CI: тесты и линт на PR
labels: epic:e0, infra
depends: DND-001

**Скоуп:** GitHub Actions workflow: на pull_request — ruff + pytest (backend) с postgres-сервисом; кэш зависимостей.
**Приёмка:** PR с падающим тестом блокируется красным чеком; зелёный PR проходит за разумное время.

### DND-003 · Каркас frontend
labels: epic:e0, frontend
depends: DND-001

**Скоуп:** `frontend/` — Vite + React 18 + TS; React Router с картой маршрутов из AR §7 (страницы-заглушки); тёмная тема по умолчанию; API-клиент (fetch-обёртка с базовым URL и обработкой `{error:{code,message}}`); i18next с locale `ru` и словарём; TanStack Query подключён. CI из DND-002 дополнить фронт-джобой (tsc + build).
**Вне скоупа:** реальные экраны.
**Приёмка:** `npm run dev` показывает shell с навигацией по заглушкам; ни одной русской строки в JSX вне словаря; build проходит в CI.

### DND-004 · CD: деплой на прод-сервер
labels: epic:e0, infra
depends: DND-002

**Скоуп:** workflow деплоя по merge в main: build образов, доставка на self-hosted runner, `docker compose up -d`, миграции, healthz-проверка. Схема — как в существующем проде владельца (Cloudflare перед сервером).
**Приёмка:** merge в main приводит к обновлению прода без ручных шагов; неудачный healthz валит деплой.

---

## EPIC US-1 · Регистрация и вход

### DND-010 · Auth backend: email + JWT
labels: epic:us-1, backend
depends: DND-001

**Скоуп:** таблицы `users`, `oauth_accounts` (AR §5) + миграция; регистрация email+пароль (argon2), login, refresh (httpOnly cookie), logout, `GET /me`; JWT access 15 мин; зависимость `current_user` для остальных модулей.
**Вне скоупа:** OAuth, отправка писем.
**Приёмка:** тесты: регистрация, логин с неверным паролем, refresh-ротация, доступ к /me с/без токена; `email_verified=false` у новых пользователей.

### DND-011 · Верификация email
labels: epic:us-1, backend
depends: DND-010

**Скоуп:** выдача токена верификации, отправка письма через конфигурируемый SMTP, `GET /auth/verify-email?token=`; повторная отправка письма; без верификации доступ к API закрыт (кроме auth-эндпоинтов), с понятной ошибкой `email_not_verified`.
**Приёмка:** тесты с мок-SMTP: happy path, просроченный/битый токен, повторная отправка.
**Примечание:** гейтинг реализован как переиспользуемая зависимость `get_verified_user` (покрыта юнит-тестом), но пока не подключена ни к одному роуту — в кодовой базе ещё нет не-auth эндпоинтов. Реальное подключение — при появлении первого такого роута (DND-020+).

### DND-012 · OAuth: Яндекс
labels: epic:us-1, backend
depends: DND-010

**Скоуп:** `GET /auth/oauth/yandex/authorize` + callback по AR §6; связка с существующим аккаунтом по email или создание нового (`email_verified=true`); провайдер активен только при наличии ключей в env.
**Приёмка:** тесты с мок-провайдером: новый пользователь, существующий email, повторный вход.

### DND-013 · OAuth: VK ID
labels: epic:us-1, backend
depends: DND-012

**Скоуп:** аналогично DND-012 для VK ID, с учётом того, что VK может не отдавать email (тогда — запрос email у пользователя на шаге завершения регистрации).
**Приёмка:** тесты: с email, без email (доп. шаг), повторный вход.

### DND-014 · OAuth: Mail.ru
labels: epic:us-1, backend
depends: DND-012

**Скоуп:** аналогично DND-012 для Mail.ru.
**Приёмка:** тот же набор тестов, что в DND-012.

### DND-015 · Auth frontend + дашборд
labels: epic:us-1, frontend
depends: DND-003, DND-011

**Скоуп:** страницы входа/регистрации/«подтвердите почту»; кнопки OAuth (рендерятся по списку активных провайдеров из API); хранение сессии, авторедирект гостя на /login; дашборд `/app`: три списка-заглушки (персонажи, кампании, торговцы) на реальных запросах.
**Приёмка:** полный проход регистрация→письмо→верификация→логин руками на дев-стенде; BR US-1 закрыт совместно с DND-010…014.

---

## EPIC US-12 · Игровой контент

### DND-020 · Модели контента + миграция
labels: epic:us-12, backend
depends: DND-001

**Скоуп:** таблицы `races, classes, class_levels, subclasses, spells, spell_classes, items, backgrounds` по AR §5 (slug+locale уникальность, JSONB `data`) + Pydantic-схемы.
**Приёмка:** миграция накатывается/откатывается; модельные тесты на уникальность (slug, locale).

### DND-021 · Импорт контент-паков
labels: epic:us-12, backend
depends: DND-020

**Скоуп:** `ContentImportService`: JSON-пак, валидация, upsert по slug, отчёт `{created, updated, errors[]}`; частично битый пак не применяется вовсе (транзакция). Источник пака — файл `CONTENT_PACK_PATH`, читается на старте приложения (`app/content/pack_loader.py`); загрузка нового пака — только через браузерную админку, которая перезаписывает файл. HTTP-эндпоинта импорта в API нет (убран: контент не пользовательская функция).
**Приёмка:** тесты: полный пак, повторный импорт (updated), пак с ошибкой (rollback + отчёт), старт с файлом/без файла/с битым файлом. BR US-12.

### DND-022 · Read-API справочников
labels: epic:us-12, backend
depends: DND-020

**Скоуп:** GET-эндпоинты AR §6 (расы, классы с уровнями, заклинания с фильтрами class/level, предметы с фильтром type, фоны); in-process TTL-кэш, сбрасываемый импортом.
**Приёмка:** тесты фильтров; после импорта кэш отдаёт свежее.

### DND-023 · JSON-схема пака + пример
labels: epic:us-12, backend, docs
depends: DND-021

**Скоуп:** `docs/content-pack.schema.json` + `docs/examples/content-pack.example.json` — минимальный валидный пак (1 раса, 1 класс с уровнями 1–3, 3 заклинания, 5 предметов, 1 фон) на русском. Это шаблон, по которому владелец готовит реальный контент.
**Приёмка:** пример проходит импорт DND-021 без ошибок; схема соответствует Pydantic-валидации.

---

## EPIC US-2 · Создание персонажа

### DND-030 · Модели персонажа + миграция
labels: epic:us-2, backend
depends: DND-020

**Скоуп:** таблицы `characters, character_spells, inventory_entries, level_up_records` по AR §5 + миграция.
**Приёмка:** миграция обратима; FK-целостность покрыта тестами.

### DND-031 · rules_5e
labels: epic:us-2, backend
depends: DND-001

**Скоуп:** `app/characters/rules_5e.py` — чистые функции по AR §3: модификаторы, бонус мастерства, XP-пороги 1–20, инициатива, пассивная внимательность, базовый КД, расчёт хитов при level-up (среднее/бросок).
**Приёмка:** юнит-тесты по официальным таблицам 5e на все функции, граничные значения (score 1, 30; уровень 1, 20).

### DND-032 · Character API + computed
labels: epic:us-2, backend
depends: DND-030, DND-031, DND-022

**Скоуп:** CRUD персонажа (AR §6), включая PATCH произвольных полей (xp, деньги, hp — по принципу BR §4.1); блок `computed` в GET; проверка владения во всех методах; поле `level` через PATCH не редактируется (ошибка `level_direct_edit_forbidden`).
**Приёмка:** тесты: computed корректен для эталонного персонажа; чужой персонаж — 404/403 (IDOR); PATCH level отклоняется. BR US-2 (backend-часть).

### DND-033 · Мастер создания: раса, класс, фон
labels: epic:us-2, frontend
depends: DND-015, DND-032

**Скоуп:** первые шаги визарда `/app/characters/new`: выбор расы/класса/фона карточками с описаниями из контента; состояние визарда локально (черновиков на бэке нет).
**Приёмка:** данные шагов доезжают до финального POST (проверяется в DND-034); mobile-вёрстка.

### DND-034 · Мастер создания: характеристики и финал
labels: epic:us-2, frontend
depends: DND-033

**Скоуп:** шаг характеристик: point buy (27 очков), standard array, броски 4d6 drop lowest, ручной ввод; шаг деталей (имя, мировоззрение, внешность, предыстория); превью с computed; POST и редирект на лист.
**Приёмка:** все четыре метода дают валидный POST; point buy не даёт потратить >27; BR US-2 закрыт end-to-end.

---

## EPIC US-3 + US-11 · Лист персонажа

### DND-040 · Инвентарь и заклинания: backend
labels: epic:us-3, backend
depends: DND-032

**Скоуп:** эндпоинты инвентаря (добавить по item_id ИЛИ custom_name — BR US-11; qty, equipped, удаление) и `PUT /characters/{id}/spells` (известные/подготовленные) по AR §6.
**Приёмка:** тесты: custom item без item_id; equipped-флаг; заклинание не из списка класса — ошибка; IDOR-проверки.

### DND-041 · Лист: главный таб
labels: epic:us-3, frontend
depends: DND-015, DND-032

**Скоуп:** `/app/characters/{id}`, mobile-first: характеристики с модификаторами, спасброски, навыки, КД/инициатива/скорость, HP (редактируемые current/temp), заметки; баннер «доступен новый уровень» по `computed.level_up_available` (ведёт на визард, сам визард — DND-052).
**Приёмка:** ручная проверка на телефоне (ширина 375px) — всё читаемо и редактируемо; PATCH-поля сохраняются. BR US-3 (частично).

### DND-042 · Лист: инвентарь и кошелёк
labels: epic:us-3, frontend
depends: DND-041, DND-040

**Скоуп:** табы «Инвентарь» (список, добавление из справочника с поиском и как custom-строка, qty/equipped) и «Кошелёк» (три числа, прямое редактирование).
**Приёмка:** BR US-3 и US-11 закрыты; кошелёк редактируется только здесь (не на витрине — проверяется в DND-074).

### DND-043 · Лист: книга заклинаний
labels: epic:us-3, frontend
depends: DND-041, DND-040

**Скоуп:** таб «Заклинания»: слоты по уровням из computed; списки известных/подготовленных; добавление из справочника с фильтром по классу и уровню; карточка заклинания.
**Приёмка:** кастер видит слоты, не-кастер — пустое состояние с пояснением; BR US-3 закрыт полностью.

---

## EPIC US-4 + US-5 · Прокачка и откат

### DND-050 · Level-up backend
labels: epic:us-4, backend
depends: DND-032

**Скоуп:** `POST /characters/{id}/level-up` по AR §4.3/§6: валидация порога XP, состав дельты (hp_method, asi|feat, subclass на нужном уровне, spells_learned), применение к персонажу, запись LevelUpRecord.
**Приёмка:** тесты: level-up без XP — `level_up_not_available`; ASI и feat одновременно — ошибка; подкласс не на своём уровне — ошибка; дельта полна и воспроизводима. BR US-4 (backend).

### DND-051 · Rollback + история backend
labels: epic:us-5, backend
depends: DND-050

**Скоуп:** `POST /level-rollback` (только верхняя запись, LIFO), `GET /level-history`; обратное применение дельты, включая забытые заклинания.
**Приёмка:** тесты: up→rollback возвращает точное исходное состояние (полное сравнение); rollback на пустом стеке — `rollback_empty`; двойной rollback невозможен. BR US-5 (backend).

### DND-052 · Визард level-up (frontend)
labels: epic:us-4, frontend
depends: DND-041, DND-050

**Скоуп:** `/app/characters/{id}/level-up` в стиле BG3: экран «уровень N → N+1», что открывается (фичи уровня из class_levels), шаги: хиты (среднее/бросок с вводом), ASI или черта, подкласс (если открывается), заклинания; подтверждение с итоговой дельтой.
**Приёмка:** прокачка 2→3 с выбором подкласса проходит end-to-end; BR US-4 закрыт.

### DND-053 · История уровней + откат (frontend)
labels: epic:us-5, frontend
depends: DND-052, DND-051

**Скоуп:** таб «История уровней»: список дельт человекочитаемо; кнопка отката у верхней записи с подтверждением («будет отменено: …»).
**Приёмка:** BR US-5 закрыт end-to-end.

### DND-054 · Ввод XP на листе персонажа
labels: epic:us-4, frontend, backend
depends: DND-041, DND-050

**Контекст:** US-4 начинается со слов «вписываю полученный XP», но поля для XP на листе не было: `computed.level_up_available` не мог стать `true` через интерфейс, и визард прокачки (DND-052) оставался недостижим — попасть в него можно было только PATCH-ом через API.
**Скоуп:** секция «Опыт и уровень» на главном табе листа: поле ввода XP (сохраняется общей кнопкой «Сохранить» вместе с HP и заметками), текущий уровень, полоса прогресса до следующего уровня и подпись «X из Y XP до N уровня»; на 20 уровне вместо полосы — пояснение. Границы полосы приходят из `computed` (`xp_level_floor`, `xp_next_threshold`) — фронт не дублирует таблицу порогов 5e.
**Приёмка:** введённый XP уходит в PATCH и возвращается на лист; при пересечении порога появляется баннер «Доступен новый уровень!» со ссылкой на визард; на 20 уровне полосы прогресса нет. Тесты: сохранение XP, отрисовка прогресса, состояние максимального уровня, границы `computed` (backend).

---

## EPIC US-6 + US-8 + US-9 · Кампании

### DND-060 · Кампании backend
labels: epic:us-8, backend
depends: DND-032

**Скоуп:** таблицы campaigns, campaign_characters уже есть (DND-030? — нет, они в AR §5, добавить миграцией здесь); CRUD кампании, invite_code (генерация + пересоздание), `POST /campaigns/join {invite_code, character_id}`, исключение/выход, список «мои кампании» (как DM и как игрок).
**Приёмка:** тесты: join чужим персонажем — ошибка; повторный join — идемпотентная ошибка; пересоздание кода отсекает старый; kick доступен только DM. BR US-8, US-6 (backend).

### DND-061 · DM read-only доступ к листам
labels: epic:us-9, backend
depends: DND-060

**Скоуп:** `GET /campaigns/{id}/characters/{character_id}` — полный лист + computed, доступен только DM этой кампании и только для присоединённых персонажей.
**Приёмка:** тесты: DM видит; не-DM — 403; персонаж вне кампании — 404. BR US-9 (backend).

### DND-062 · Кампании frontend
labels: epic:us-8, frontend
depends: DND-015, DND-061

**Скоуп:** создание/редактирование кампании (название, описание, дата и место следующей игры); страница кампании для DM: инвайт-код с копированием, список персонажей → read-only листы; страница `/app/campaigns/join`: ввод кода + выбор персонажа; у игрока кампания видна на дашборде.
**Приёмка:** BR US-6, US-8, US-9 закрыты end-to-end.

---

## EPIC US-7 + US-10 · Торговцы

### DND-070 · Торговцы backend: CRUD
labels: epic:us-10, backend
depends: DND-032

**Скоуп:** таблицы merchants, merchant_items + миграция; CRUD торговца (share_code, is_open), позиции (item_id, override-цена по трём валютам или null, quantity или null=∞).
**Приёмка:** тесты владения; закрытая лавка недоступна для операций.

### DND-071 · Витрина и покупка
labels: epic:us-7, backend
depends: DND-070

**Скоуп:** `GET /shop/{share_code}` — без авторизации (BR §4.5); `POST /shop/{code}/buy {character_id, merchant_item_id, quantity}` — одна транзакция, `SELECT FOR UPDATE` на персонаже и позиции; списание строго по валютам цены без конвертации.
**Приёмка:** тесты: недостаток средств (`insufficient_funds`), нехватка стока, чужой персонаж (`not_your_character`), закрытая лавка; конкурентный тест — два одновременных buy последней единицы: ровно один успешен. BR US-7 (покупка).

### DND-072 · Продажа торговцу
labels: epic:us-7, backend
depends: DND-071

**Скоуп:** `POST /shop/{code}/sell {character_id, inventory_entry_id, quantity}` — 50% цены карточки предмета, округление вниз по каждой валюте; custom items непродаваемы; предмет исчезает (в сток не попадает — BR §6).
**Приёмка:** тесты: расчёт 50% с округлением (нечётные цены), продажа custom item — ошибка, продажа части стака. BR US-7 (продажа).

### DND-073 · Конструктор торговца (frontend)
labels: epic:us-10, frontend
depends: DND-015, DND-070

**Скоуп:** `/app/merchants/{id}`: карточка (имя, описание, открыт/закрыт), позиции с добавлением из справочника, override-цены, количество; блок «ссылка для игроков» с копированием.
**Приёмка:** BR US-10 закрыт.

### DND-074 · Витрина (frontend)
labels: epic:us-7, frontend
depends: DND-042, DND-072

**Скоуп:** `/shop/{code}`: гость — просмотр + приглашение войти; игрок — селектор своего персонажа, кошелёк read-only, покупка и продажа с подтверждениями. Жёсткие UX-барьеры BR §4.1: ни одной ссылки на редактирование листа; при уходе со страницы — предупреждение и сброс сессии торговли.
**Приёмка:** ручная проверка с телефона; BR US-7 закрыт end-to-end.

---

## EPIC E9 · Релиз

### DND-080 · Лендинг
labels: epic:e9, frontend
depends: DND-003

**Скоуп:** публичная `/`: что это, для игрока / для мастера, CTA на регистрацию. Тёмная фэнтези-стилистика, без стоковой мишуры.
**Приёмка:** lighthouse mobile ≥ 90 по perf/a11y; единственная публичная страница кроме витрины (BR §3).

### DND-081 · Security-аудит: IDOR
labels: epic:e9, backend
depends: DND-074, DND-062, DND-053

**Контекст:** была единой задачей на весь чеклист (IDOR + JWT + rate-limit + заголовки + секреты) — агент трижды подряд падал по `error_max_turns` (100), не успевая дойти даже до отчёта. Разбита на последовательную цепочку по областям чеклиста; каждая часть — свой прогон и свой PR, дополняющий один и тот же `docs/security-report.md` (отсюда `depends` цепочкой, а не веером — параллельные PR иначе конфликтовали бы на одном файле).
**Скоуп:** IDOR на всех ресурсах (персонаж/кампания/торговец/инвентарь/покупка) — владение проверяется в каждом сервис-методе (правило 7 CLAUDE.md).
**Приёмка:** создан `docs/security-report.md` с разделом IDOR; найденные проблемы исправлены в этом же PR или заведены ишью с меткой security.

### DND-083 · Security-аудит: JWT и rate-limit
labels: epic:e9, backend
depends: DND-081

**Скоуп:** JWT (срок жизни access, ротация refresh), rate-limit на `/auth/*` и `/buy`.
**Приёмка:** раздел «JWT и rate-limit» дополнен в `docs/security-report.md`; найденные проблемы исправлены в этом же PR или заведены ишью с меткой security.

### DND-084 · Security-аудит: заголовки и секреты
labels: epic:e9, backend
depends: DND-083

**Скоуп:** заголовки безопасности (CSP, HSTS и т.п.), секреты вне репозитория (правило 12 CLAUDE.md — никаких дефолтов на известные креды).
**Приёмка:** раздел «Заголовки и секреты» дополнен в `docs/security-report.md` — чеклист закрыт полностью; найденные проблемы исправлены в этом же PR или заведены ишью с меткой security.

### DND-082 · Приёмочный прогон и релиз
labels: epic:e9, infra, manual
depends: DND-084, DND-004

**Скоуп:** сценарный прогон всех критериев BR §7 на проде (можно тестовыми аккаунтами); фиксация результатов в `docs/acceptance-run.md`.
**Приёмка:** все 6 пунктов BR §7 отмечены выполненными.