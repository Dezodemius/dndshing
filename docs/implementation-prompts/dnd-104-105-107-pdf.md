# Printable character sheet: implementation prompt

## Source and stopping point

User request: continue Claude's work on downloading the character PDF; first write the prompt, then implement using smaller OpenAI models. Do not invoke Claude.

Repository: Dezodemius/dndshing. Base: origin/develop at 34c48f6 (merged PR #184, DND-103). GitHub has no standalone issues for DND-104/105/107; their canonical acceptance criteria are in the versioned .claude/docs/BACKLOG.md. Claude's feat/dnd-104-108 worktree contains only four untracked reference PNGs, no implementation. Those PNGs were copied without changes.

## Outcome and design

Implement DND-104, DND-105, DND-107 together as the complete owner PDF flow. Open /app/characters/:id/sheet from the character detail screen. Fetch GET /characters/:id/sheet using the existing authenticated API client. Render a light paper document with four A4 pages, outside Layout but behind RequireAuth. The same React document provides desktop, 375px single-column mobile reflow, and print. Download PDF calls window.print(), letting the browser save PDF as required by ARCHITECTURE 4.8. The fourth page always exists, including noncasters, matching the reference. No new PDF library or server rendering service.

Port structure and geometry from git show ace590c^:features/characters/components/character-sheet.tsx and the related legacy CSS/helpers. Preserve the named blocks, ordering, and columns from the four reference PNGs. Do not port obsolete Next.js dependencies or client-side D&D formulas. Read all four PNGs before layout work. Only gold/silver/copper appear. Portrait upload and a DM print screen remain separate tasks DND-106 and a later task; provide the reference's empty appearance/portrait area without pretending upload exists.

## Contract and editing

- Read CLAUDE.md, .claude/docs/{CLAUDE,REQUIREMENTS,ARCHITECTURE,BACKLOG}.md and .claude/skills/{code-style,ux-convention,security-review}.md.
- Mirror backend CharacterRead/CharacterUpdate/CharacterSheetRead in frontend types. Existing detail fixtures must remain compatible without weakening the sheet endpoint's complete contract.
- Use computed for all rules: modifiers, saves, skills, AC, effective stats/HP/speed, hit dice, spellcasting ability/DC/attack and slots. Never duplicate 5e formulas. Abilities/level stay read-only.
- Use react-hook-form and zod, explicit Save, visible dirty/saving/error/success state. PATCH only editable values with server-compatible limits. No field-level debounce autosave. Preserve content when merging PATCH response into ['character', id, 'sheet']; update/invalidate existing ['character', id] and ['characters'] caches appropriately. Preserve unsaved input on errors/refetch; prevent printing stale/invalid data, save first when printing a dirty valid form. Prevent duplicate save/print requests.
- Spell slots spent edit levels 1-9 with capacities from computed. Caster detection uses computed.spellcasting_ability even without slots. Support attacks and personal text fields; show suggested personality values from content background where available.
- Pure buildSheetModel must not import React/i18next. Stable dynamic IDs include inventory/spell entry keys. Export model fields as an id-to-canonical-value map; empty stays empty, checkboxes are booleans. Export formatModifier in a pure utility and reuse at existing call sites if moved.
- Mark displayed fields with data-sheet-field and blocks with data-sheet-block. Same model drives document in all modes; text fallback presentation must not corrupt canonical values. Print has no visible buttons, inputs, navigation, or scrollbars. Text must wrap; detect long-content overflow and handle it explicitly rather than silently clipping or printing unreadable text.
- All UI labels/errors through ru locale. Paper colors in theme.css. Existing app theme unaffected. Loading/error/404 states and accessible labels required.

## Verification

- Vitest: filled and empty model including fallbacks, no arithmetic duplication; form save retains content, cache consistency, API failure retains edits, validation, spent slots, window.print mock, noncaster and level-1 paladin; route authentication/layout regression.
- Playwright: authenticate with mocked API fixtures, not a production account. Compare screen and print data-sheet-field values against buildSheetModel for filled and empty sheets. Checkbox collector reads checked. Check layout blocks against a machine-readable named spec (label translation key, page, column, row, implemented/partial/out_of_scope/own_extension with linked reasons). Ensure mobile 375px does not overflow viewport and page grouping stays correct. Generate real A4 PDF and verify page count and text presence.
- Restore legacy Playwright + sharp @visual comparison, four original PNGs, tolerance 64 and threshold 0.12. Keep reference hashes immutable with SOURCE.md provenance. Visual comparison is informational in CI and honestly report mismatch; do not regenerate references to make tests green. Typecheck e2e directory.
- Run frontend build and full Vitest; appropriate Playwright structural/parity tests and visual comparison. Run project backend checks as environment allows; document any existing failure accurately. Review generated screenshots/PDF for clipping, whitespace, borders, all four pages and mobile layout.
- Preserve original encodings and CRLF in every touched/new text file. Final git diff --check and byte-level CRLF/BOM comparison. No unrelated changes, no modifications to .gitattributes, no secrets, no deployment or merge.

## Delegation boundaries

Implementation uses gpt-5.6-terra agents: (A) API types, pure model/form schema and model unit tests; (B) document UI, route, locales/styles and component tests; (C) Playwright fixtures/config, layout/parity/visual tests and informational CI job. Share contracts before dependent work. Each agent edits only its assigned files. Root owns integration/review, prompt, dependency install, final checks and corrections through implementation agents. No artificial money budget caps. No commits or pushes until root reviews the complete diff.
